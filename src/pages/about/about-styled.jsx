import styled from 'styled-components'
import {maxWidth, greenFin} from '../../components/variables'
export const AboutContainer = styled.div`
    max-width: ${maxWidth};
    margin: 0 auto;
    width:100%;
    background-color: ${greenFin};
    padding: 4rem 0;
 
`